<%= turbo_stream_from "organizations" %>
<main class="container">
  <div class="header">
    <h1>Organizations</h1>
  </div>
  <% if @organizations.none? %>
    <%= render "empty_state" %>
  <% end %>
  <%= turbo_frame_tag "organizations" do %>
    <%= render @organizations %>
  <% end %>
</main>