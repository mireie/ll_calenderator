<%= turbo_frame_tag league do %>
  <div class="league card">
    <%= link_to league.title, league, data: { turbo_frame: "_top" } %>
    <div class="card__body">
      <p><%= league.description %></p>
      <p><%= league.days %></p>
      <p><%= league.sport %></p>
    </div>
  </div>
<% end %>